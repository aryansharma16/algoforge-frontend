# API base URL (must include /api). Restart dev server after changing.
# Local backend:
VITE_API_URL=http://localhost:5123/api
# Remote backend (e.g. EC2) — backend must enable CORS for your frontend origin:
# VITE_API_URL=http://35.154.216.23:5123/api
